using System.Drawing;
using UnityEngine;

public class R_and_B_Angles : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
      
    }

    // Update is called once per frame
    void Update()
    {

        float redAngle = 30;
        float blueAngle = 60;

        //redVector = CalculateVectorFrontAngle(redAngle);
      //  blueVector = CalculateVectorFrontAngle(blueAngle);

      //  Debug.DrawLine(Vector3.zero, redVector, Color.red);
      //  Debug.DrawLine(Vector3.zero, blueVector, Color.red);


    }
}
